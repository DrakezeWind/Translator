[project]
name = "translator"
version = "0.1.0"
description = "This is a test translator learning how to code in python"
requires-python = ">=3.10"
dependencies = [
    "deep-translator>=1.11.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
