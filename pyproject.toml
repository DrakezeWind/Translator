[project]
name = "translator"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "deep-translator>=1.11.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
